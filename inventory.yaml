groups:
  - name: nixagents
    targets: 
    - nixagent1
    - nixagent2
    config:
      transport: ssh
      ssh:
        private-key: /root/.ssh/id_rsa
config:
  transport: ssh
  ssh:
    host-key-check: false